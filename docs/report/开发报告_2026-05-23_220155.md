# 开发报告

> 时间：2026-05-23 22:01:55
>
> 需求：3 项

---

## 需求 1: 教师教学班级切换增加学期/专业筛选

**实现：** 侧边栏课程切换器上方新增「学期」和「专业」下拉筛选框，可选值从后端加载。`filteredTeacherClasses` 计算属性按筛选条件过滤班级列表。

## 需求 2: 考核点多目标绑定 + 成绩权重百分比

**数据库：** `docs/database/migrate_assessment_multi_objective.sql`
- 新建 `assessment_objective` 表（考核点-目标多对多）
- `assessment_point` 新增 `weight_percent` 字段

**后端：**
- `AssessmentObjective` 实体 + Mapper
- `AssessmentPoint` 新增 `weightPercent` + `objectiveIds` 透传字段
- `AssessmentService` 重构：`createAssessment/updateAssessment` 支持 `objectiveIds` 列表写入关联表；`listAssessments` 回填 `objectiveIds`；前端保存时校验 `Σ weightPercent = 100`

**前端：**
- `AssessmentMap.vue`：考核点表单改为多选目标 + 权重%输入；表格列显示所有绑定目标标签；顶部实时显示权重总和（=100 绿色，≠100 红色）

**成绩计算公式：** 学生总成绩 = Σ(score_i × weightPercent_i / 100)

## 需求 3: 课程级计算展示目标编号和指标点编号

**后端：** `CourseCalcService.CalcResult` 新增 `objectiveLabels` 和 `indicatorLabels`，填入 `obj_no`（如 1-1）和 `indicator_no`（如 3-1）。

**前端：** `CourseCompute.vue` 使用 label 映射显示实际编号，而非内部 ID。

---

## 执行步骤

```bash
mysql -u root -p < docs/database/migrate_assessment_multi_objective.sql
cd backend && mvn spring-boot:run
cd frontend && npm run dev
```

## 修改文件

| 文件 | 类型 |
|------|:---:|
| `migrate_assessment_multi_objective.sql` | 新增 |
| `entity/AssessmentPoint.java` | 修改 |
| `entity/AssessmentObjective.java` | 新增 |
| `mapper/AssessmentObjectiveMapper.java` | 新增 |
| `service/AssessmentService.java` | 修改 |
| `service/CourseCalcService.java` | 修改 |
| `layouts/AdminLayout.vue` | 修改 |
| `views/teacher/AssessmentMap.vue` | 重写 |
| `views/teacher/CourseCompute.vue` | 修改 |

---

**编写人：** Claude Code (AI Assistant)
