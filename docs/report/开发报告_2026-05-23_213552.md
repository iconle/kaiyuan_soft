# 开发报告

> 时间：2026-05-23 21:35:52
>
> 修复项：2 项

---

## Issue 1: 新班级成绩导入只显示学生名单无成绩列

**原因：** 「计算机网络-2025秋-1班」无 `course_outline` → 无 `assessment_point` → `headers` 为空 → 表格仅显示学号/姓名列，无成绩输入列。

**修复：** 当 `headers.length === 0` 且有学生数据时，显示引导提示：「该课程尚未配置考核点，请先完成课程大纲配置」，并提供「前往配置课程目标」快捷按钮。

## Issue 2: 成绩录入增加编辑保存功能

**变更：** 将即时自动保存改为批量保存模式：

- 编辑时数据暂存本地 `edits` 映射，不立即调 API
- 右上角显示「保存修改 (N)」按钮（有未保存修改时出现）
- 点击后批量提交所有修改，完成后刷新数据
- 避免每次输入都调 API 的性能问题

## 修改文件

| 文件 | 变更 |
|------|------|
| `views/teacher/ScoreImport.vue` | 重写：无考核点引导 + 批量保存模式 |

---

**编写人：** Claude Code (AI Assistant)
