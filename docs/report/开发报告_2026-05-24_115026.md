# 开发报告

> 时间：2026-05-24 11:50:26
>
> 类型：题目设置模块 + 成绩导入重构

---

## 需求

1. 考核点题目表关联到考核点（→大纲→教学班级，链路完整无需额外字段）
2. 新增「题目设置」导航模块，用于管理考核点的题目和题目-目标绑定
3. 成绩导入重构：按考核点切换，每题单独打分，显示目标绑定，自动计算总成绩

## 后端新增

| 文件 | 说明 |
|------|------|
| `controller/QuestionController.java` | `/api/assessments/{id}/questions` CRUD |
| `service/QuestionService.java` | 题目增删改查 + 目标绑定管理 |
| `service/ScoreService.java` | `updateScore` 支持 questionId 参数 |
| `controller/ScoreController.java` | updateScore 解析 questionId |
| `entity/StudentScore.java` | 新增 questionId 字段 |
| ScoreCell record | 新增 questionId 字段 |

## 前端新增/修改

| 文件 | 类型 | 说明 |
|------|:---:|------|
| `views/teacher/QuestionSetup.vue` | 新增 | 按考核点管理题目：新增/编辑/删除，多目标绑定 |
| `views/teacher/ScoreImport.vue` | 重写 | 考核点下拉切换 → 每题独立打分+目标显示 → 自动总成绩列 → 批量保存 |
| `router/index.js` | 修改 | 新增 `/teacher/:classId/questions` 路由 |
| `layouts/AdminLayout.vue` | 修改 | 新增「题目设置」菜单项 |

## 成绩导入新布局

```
[考核点下拉: 期末笔试 → 目标1-1,1-2,1-3]  [下载模板]  [状态]

学号 | 姓名 | 大题1(满分20→1-1) | 大题2(满分30→1-2) | 大题3(满分50→1-1,1-2) | 总成绩
001  | 张三 |    18    |    25    |    45    |   88
```

无题目时退回考核点整体打分模式。

## 执行步骤

```bash
mysql -u root -p < docs/database/migrate_assessment_questions.sql
cd backend && mvn spring-boot:run
```

---

**编写人：** Claude Code (AI Assistant)
