# 开发报告

> 时间：2026-05-24 17:26:36
>
> 类型：宏观看板新增达成度详情弹窗

---

## 需求

点击宏观看板表格行，弹窗显示该教学班级的课程目标达成度（第一级）和课程级指标点达成度（第二级）。

## 实现

`GlobalDashboard.vue`：
- 表格新增 `highlight-current-row` + `@row-click="showDetail"`
- `showDetail(row)` 调用 `GET /api/classes/{classId}/compute/results` 获取达成度数据
- 弹窗分左右两栏：课程目标达成度（第一级）+ 课程级指标点达成度（第二级）
- 目标编号/指标点编号使用 `objectiveLabels`/`indicatorLabels` 映射
- 未执行计算的课程显示提示文字

## 修改文件

`views/academic/GlobalDashboard.vue` — 新增行点击 + 详情弹窗

---

**编写人：** Claude Code (AI Assistant)
