# 开发报告

> 时间：2026-05-24 16:46:03
>
> 修复项：2 项

---

## Issue 1: 指标点列显示编号而非 ID

**修复：** `onMounted` 中调用 `listGradReqs` 加载毕业要求及指标点，构建 `indicatorLabels: { [id]: indicator_no }` 映射。`resultData` 计算属性使用 `indicatorLabels[id]` 显示如"3-1"。

## Issue 2: 去掉学期选择下拉框

**修复：** 移除学期 `<el-select>` 和 `selectedSemesterId` 变量。所有 API 调用（`getGlobalResults` / `triggerGlobalCompute` / `getRadarData` / `downloadMajorExcel`）的 `semesterId` 参数固定为 `1`。

## 修改文件

`views/director/GlobalCompute.vue` — 学期下拉移除 + 指标点编号显示

---

**编写人：** Claude Code (AI Assistant)
