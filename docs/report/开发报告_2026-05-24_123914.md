# 开发报告

> 时间：2026-05-24 12:39:14
>
> 修复项：2 项

---

## Issue 1: 题目目标仅限考核点已绑定的目标

**实现：**
- `parentObjectiveIds` 计算属性：从当前选中考核点的 `objectiveIds`（assessment_objective N:M 绑定）提取
- `parentObjectives` 过滤 `allObjectives` 仅保留父考核点绑定的目标
- 题目表单的目标下拉框数据源改为 `parentObjectives`

## Issue 2: 题目总分校验（Σ=100）

**前端：**
- `questionSum` 计算属性：实时显示当前考核点所有题目满分总和
- 顶部显示 `题目总分: XX ✓/✗ 必须等于100`
- `handleSubmit` 前校验：新总分不得超过100

**后端：**
- `QuestionService.validateScoreSum()`：创建/编辑时计算该考核点所有题目满分和，超过100抛 `BizException`

## 修改文件

| 文件 | 类型 |
|------|:---:|
| `views/teacher/QuestionSetup.vue` | 修改 |
| `service/QuestionService.java` | 修改 |

---

**编写人：** Claude Code (AI Assistant)
