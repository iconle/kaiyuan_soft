# 开发报告

> 时间：2026-05-24 13:08:15
>
> 修复项：2 项

---

## Issue 1: 下拉框显示数字ID而非目标编号

**根因：** `loadAll()` 从 `getScores()` 的 `assessments` 字段读取考核点列表，该接口返回的 `AssessmentPoint` 未填充 `objectiveIds`（N:M 绑定），`assessmentObjLabel` 退化为显示原始数字 ID。

**修复：** `loadAll()` 改用 `listAssessments()` 加载考核点列表（该接口经由 `AssessmentService` 已填充 `objectiveIds`）。下拉框正确显示 `"考核点名 → 1-1,1-2"`。

## Issue 2: 题目不显示

**根因同上：** `objectiveIds` 为空导致题目级 UI 判断逻辑异常。

**修复：** Issue 1 修复后自动解决。

## 修改文件

`views/teacher/ScoreImport.vue` — 考核点数据源从 `getScores` 切换为 `listAssessments`

---

**编写人：** Claude Code (AI Assistant)
