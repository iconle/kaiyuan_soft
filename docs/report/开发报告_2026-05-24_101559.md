# 开发报告

> 时间：2026-05-24 10:15:59
>
> 修复项：3 项

---

## Issue 1: 学生管理开放给教务 + 删除旧学生名单

**变更：**
- 路由 `/admin/students` 角色从 `['ADMIN']` 改为 `['ADMIN','ACADEMIC']`
- 删除旧路由 `/academic/students`（StudentImport）
- 侧边栏：教务管理子菜单中「学生名单」改为「学生管理」，指向 `/admin/students`

## Issue 2: 课程体系教学班级弹窗教师列不显示

**根因：** `CourseService.listClasses()` 仅填充 `courseName`，未查 `sys_user` 填充 `teacherName`。

**修复：** 注入 `SysUserMapper`，在遍历班级时查教师姓名填入 `teacherName`。

## Issue 3: 宏观看板提示精简

**变更：** 将"还有 N 门课程未完成：[班级ID标签列表]"改为仅显示"还有 N 门课程未完成课程级计算"，移除班级 ID 标签。

## 修改文件

| 文件 | 类型 |
|------|:---:|
| `router/index.js` | 修改 |
| `layouts/AdminLayout.vue` | 修改 |
| `service/CourseService.java` | 修改 |
| `views/academic/GlobalDashboard.vue` | 修改 |

---

**编写人：** Claude Code (AI Assistant)
