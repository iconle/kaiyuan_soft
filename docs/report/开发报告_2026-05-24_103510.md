# 开发报告

> 时间：2026-05-24 10:35:10
>
> 修复项：2 项

---

## Issue 1: 宏观看板去按钮 + 专业级计算开放给教务

**变更：**
- `GlobalDashboard.vue` — 移除「执行专业级计算」按钮
- 路由 `/director/global-compute` 角色改为 `['DIRECTOR','ACADEMIC']`
- 侧边栏教务管理子菜单新增「专业级计算」菜单项

## Issue 2: 支撑矩阵提交重复键异常

**根因：** `MacroMatrixService.updateMatrix()` 对无 `id` 的条目执行 `insert`，但前端不传 `id`，导致所有条目走 insert → 与已有记录 `(course_id, indicator_id)` 唯一键冲突。

**修复：** 改为先按 course_id 删旧 → 全部 re-insert。保证每次提交为全量替换，消除重复键。

## 修改文件

| 文件 | 类型 |
|------|:---:|
| `views/academic/GlobalDashboard.vue` | 修改 |
| `router/index.js` | 修改 |
| `layouts/AdminLayout.vue` | 修改 |
| `service/MacroMatrixService.java` | 修改 |

---

**编写人：** Claude Code (AI Assistant)
