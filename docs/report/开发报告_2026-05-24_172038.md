# 开发报告

> 时间：2026-05-24 17:20:38
>
> 修复项：2 项

---

## Issue 1: 教务管理员专业级计算权限

**诊断：** 路由 `/director/global-compute` 的 `meta.roles` 已含 `['DIRECTOR','ACADEMIC']`，`GlobalCalcController` 端点均为 `hasAnyRole('DIRECTOR','ACADEMIC')`，侧边栏「专业级计算」对 ACADEMIC 可见（`v-if="isAcademic"`）。权限配置完整，刷新浏览器即可正常访问。

## Issue 2: 宏观看板增加主讲教师和开课学期列

**后端：** `CourseStatus` record 新增 `teacherName`/`semesterName` 字段，`getDashboard()` 中注入 `SysUserMapper`/`SysDictSemesterMapper` 填充教师姓名和学期名称。

**前端：** 表格新增「主讲教师」「开课学期」两列，宽度改为 `min-width` 自适应撑满，避免挤在左侧。

## 修改文件

| 文件 | 类型 |
|------|:---:|
| `service/GlobalCalcService.java` | 修改 |
| `views/academic/GlobalDashboard.vue` | 修改 |

---

**编写人：** Claude Code (AI Assistant)
